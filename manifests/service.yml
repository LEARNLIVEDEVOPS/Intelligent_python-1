apiVersion: v1
kind: Service
metadata:
    name: learnlivedevopsintelligentpython
spec:
    type: LoadBalancer
    ports:
    - port: 5000 
    selector:
        app: learnlivedevopsintelligentpython